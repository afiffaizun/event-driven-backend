// Cek
docker compose ps

docker compose up -d

// Masuk ke database
docker exec -it auth-postgres psql -U authuser authdb

\c authdb       -- Masuk ke database

\l              -- Lihat semua database
\dt             -- Lihat semua tabel
\d users        -- Lihat struktur tabel users